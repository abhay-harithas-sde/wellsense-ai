# WellSense AI - Test Environment Configuration
#
# This file is for AUTOMATED TESTING only.
# Test databases and credentials should be separate from development and production.
#
# Note: Uses test-specific secrets and separate test databases.

NODE_ENV=test
PORT=3001

# Test Database (separate from production)
DATABASE_URL="postgresql://postgres:Abhay%231709@localhost:5432/wellsense_ai_test"
MONGODB_URI="mongodb://admin:Abhay%231709@localhost:27017/wellsense_ai_test?authSource=admin"
REDIS_URL="redis://localhost:6379/1"

# JWT Configuration
JWT_SECRET=test-jwt-secret-key-for-testing-only
JWT_EXPIRES_IN=1h

# Google OAuth (test credentials)
GOOGLE_CLIENT_ID=test-google-client-id
GOOGLE_CLIENT_SECRET=test-google-client-secret
GOOGLE_CALLBACK_URL=http://localhost:3001/auth/callback

# OpenAI (test mode - use mock)
OPENAI_API_KEY=test-openai-key
OPENAI_MODEL=gpt-4o-mini
OPENAI_MAX_TOKENS=800
OPENAI_TOTAL_TOKEN_LIMIT=500000

# Feature Flags
VITE_ENABLE_AI_CHAT=true
VITE_PRODUCTION_MODE=false
