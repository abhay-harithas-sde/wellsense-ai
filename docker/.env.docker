# Docker Development Environment Configuration
# This file is used for local Docker development

# Environment
NODE_ENV=development

# PostgreSQL Configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=Abhay#1709
POSTGRES_DB=wellsense_ai
POSTGRES_PORT=5432

# MongoDB Configuration
MONGO_INITDB_ROOT_USERNAME=admin
MONGO_INITDB_ROOT_PASSWORD=Abhay#1709
MONGO_INITDB_DATABASE=wellsense_ai
MONGO_PORT=27017

# Redis Configuration
REDIS_PORT=6379

# pgAdmin Configuration
PGADMIN_DEFAULT_EMAIL=admin@wellsense.ai
PGADMIN_DEFAULT_PASSWORD=admin
PGADMIN_PORT=5050

# Mongo Express Configuration
ME_CONFIG_MONGODB_ADMINUSERNAME=admin
ME_CONFIG_MONGODB_ADMINPASSWORD=Abhay#1709
ME_CONFIG_MONGODB_URL=mongodb://admin:Abhay%231709@mongodb:27017/
ME_CONFIG_BASICAUTH_USERNAME=admin
ME_CONFIG_BASICAUTH_PASSWORD=admin
ME_CONFIG_PORT=8081

# Note: Application secrets (JWT_SECRET, GOOGLE_CLIENT_SECRET, etc.) 
# should be passed via environment variables or mounted secrets, 
# not stored in this file.
